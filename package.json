{
  "name": "ar-art-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Marker-based AR web app that animates artwork via MindAR image tracking + Three.js.",
  "scripts": {
    "serve": "node tools/serve.mjs",
    "api": "node services/gallery-api/index.mjs",
    "test": "node --test tests/*.test.mjs && npm --prefix services/gallery-api test",
    "eval": "node --test tests/*.eval.mjs"
  }
}
